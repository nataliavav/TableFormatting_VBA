Removes all formating from tables and formats them in a consistent manner. 

Empty cells are filled with "n/a" value.
Notes columns have different formatting from the rest

You will need to manually insert the code & create an excel add-in (https://trumpexcel.com/excel-add-in/)
You can also assign it to a button (Assign a macro to a button)
